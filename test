pipeline {
    agent {label 'dispatcher'}
    stages {
        stage ('test') {
            steps {
                sh 'python3 test.py'
            }
        }
        stage ('msg') {
            steps {
                echo 'MSG'
            }
        }
    }
    post {
        failure {
            echo 'FAIL'
        }
    }
}