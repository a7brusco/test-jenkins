import java.text.SimpleDateFormat
pipeline {
    agent {label 'test'}
    stages {
        stage('test'){
            steps{
                script{
                    def date = new Date()
                    def format = "dd-MM-YYYY"
                    echo date.plus(7).format(format)
                }
            }
        }
    }
}