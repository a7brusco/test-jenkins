pipeline {
    agent {label 'dispatcher'}
    stages {
        stage ('Test') {
            steps {
                script {
                    for (Project job : Hudson.getInstance().getProjects()) {
                        if (job.isBuilding() || job.isInQueue()) {
                            echo "${job.getName()}"
                        }
                    }
                }
            }
        }
    }
}