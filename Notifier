pipeline {
    agent {label 'dispatcher'}
    stages {
        stage ('Test') {
            steps {
                script {
                    def job = Jenkins.instance.getItemByFullName('test')
                    waitUntil{
                        job.isBuilding() || job.isInQueue()
                    }
                }
            }
        }
    }
}