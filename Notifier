pipeline {
    agent {label 'dispatcher'}
    stages {
        stage ('Test DB') {
            steps {
                echo 'TEST'
                sleep 10
            }
        }
    }
    post {
        always {
            if (currentBuild.result != 'ABORTED') {
                echo 'POST'
            }
        }
    }
}