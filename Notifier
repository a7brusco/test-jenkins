pipeline {
    agent {label 'dispatcher'}
    stages {
        stage ('Test') {
            steps {
                script {
                    def jobs = Jenkins.instance.getItemByFullName('job/test')
                    echo jobs.toString()
                }
            }
        }
    }
}